'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up (queryInterface, Sequelize) {
    await queryInterface.createTable('FollowUpQuestions', {
      id: {
        allowNull: false,
        autoIncrement: true,
        primaryKey: true,
        type: Sequelize.INTEGER
      },
      interview_response_id: {
        type: Sequelize.INTEGER,
        allowNull: false,
        references: {
          model: 'InterviewResponses',
          key: 'id',
        },
        onDelete: 'CASCADE', // Automatically delete follow-up questions if the response is deleted
      },
      question_text: {
        type: Sequelize.TEXT,
        allowNull: false,
      },
      media_url: {
        type: Sequelize.STRING,
        allowNull: true, // Optional field
      },
      created_by: {
        type: Sequelize.STRING,
        allowNull: false,
        defaultValue: 'LLM', // Indicates that the question was generated by the LLM
      },
      createdAt: {
        allowNull: false,
        type: Sequelize.DATE,
        defaultValue: Sequelize.literal('CURRENT_TIMESTAMP'),
      },
      updatedAt: {
        allowNull: false,
        type: Sequelize.DATE,
        defaultValue: Sequelize.literal('CURRENT_TIMESTAMP'),
      }
    });
  },

  async down (queryInterface, Sequelize) {
    await queryInterface.dropTable('FollowUpQuestions');
  }
};
